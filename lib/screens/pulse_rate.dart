// pulse_rate.dart
import 'package:flutter/material.dart';

class PulseRateScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pulse Rate Screen'),
      ),
      body: Center(
        child: Text('Pulse rate data will be displayed here'),
      ),
    );
  }
}
