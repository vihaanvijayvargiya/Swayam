
import 'package:flutter/material.dart';

class SpO2Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('SpO2 Screen'),
      ),
      body: Center(
        child: Text('SpO2 data will be displayed here'),
      ),
    );
  }
}

